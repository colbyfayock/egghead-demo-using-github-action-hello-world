name: Hello World

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: colbyfayock/egghead-demo-github-action-hello-world@v1.0.1
